# Minimum required versions for bloop development
# This file is read by setup scripts on all platforms

# Bun - suggested minimum, not strictly required
BUN_MIN_VERSION=1.3.1

# Zig - development build required for WASM features
# Format: major.minor.patch-dev.buildnum
ZIG_MIN_VERSION=0.16.0-dev.1225
