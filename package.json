{
  "name": "bloop",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*",
    "games/*"
  ],
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "ci": "bun run ci:tsc && bun test && bun run ci:jsr",
    "ci:tsc": "bun run --filter '*' ci:tsc",
    "ci:jsr": "bun run --filter '*' ci:jsr",
    "publish": "bun bin/publish.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
